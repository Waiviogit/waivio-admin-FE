export default class ContentData {
    constructor(title, index, content, modal, search, sort) {
        this.title = title;
        this.index = index;
        this.modal = modal;
        this.content = content;
        this.search = search;
        this.sort = sort;
    }
}
